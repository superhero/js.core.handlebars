{{#> layout title="Insert awesome title for the page here" }}
  <p>
    Template loaded.
    Write your markup here with support for variables, eg: "{{ foo }}"

    ---
    tests for the "if" helper
    ---
    {{# if foo '==' 'foobar' }} == {{/ if }}
    ---
    {{# if foo '!=' 'foo' }} != {{/ if }}
    ---
    {{# if i '<' 10 }} < {{/ if }}
    ---
    {{# if i '<=' 5 }} <= {{/ if }}
    ---
    {{# if i '>' 0 }} > {{/ if }}
    ---
    {{# if i '>=' 5 }} >= {{/ if }}
    ---
    {{# if i '&&' foo }} && {{/ if }}
    ---
    {{# if i '||' foo }} || {{/ if }}
    ---
    {{# if foo 'typeof' 'string' }} typeof {{/ if }}
  </p>
{{/ layout }}
